## Tuples Summary

# Tuples 
# Tuples are immutable 
# Tuples are orderd collection of items
# Tuples can store any data type
# Tuples cannot change (add or delete) values from tuples once it created
# But can add, Delete data from list which is present inside tuples.

mixed = (1, 2, 3, 4, 5, 'Six')

## No append, no pop, no insert, no remove
# Only count and index 

# Functions
# min(), max(), len(), sum()

mixed2 = (1, 2, 3, 4, 5, [6, 7, 8])
mixed2[5].pop()
print(mixed2)